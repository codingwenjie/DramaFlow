# DramaFlow 基础 API 功能开发计划

> 范围：新建项目 → 剧本生成 → 分镜生成
> 版本：v1.0 ｜ 日期：2026-08-06

---

## 1. 背景与目标

打通产品最核心的"剧本生产"链路：用户在界面配置真实 AI 模型（BYOK）后，可以
**新建项目 → 一键生成结构化剧本 → 批量生成分镜**，数据真实落库、可编辑、可复用。

本期完成后，用户不再需要手工编写幕次与镜头，且生成结果与后续的配音、合成模块
数据结构完全打通。

## 2. 现状评估

| 模块 | 已有能力 | 关键缺口 |
|------|----------|----------|
| API 配置页 | 有模型列表、Key 输入 UI | **与 `ai-models.ts` 配置存储完全脱节**，填了不生效；无连通性测试 |
| AI 服务层 | OpenAI 兼容抽象、按用途路由、mock 兜底 | 无统一错误处理、无超时、无重试 |
| 新建项目 | 对话框、`addProject`、跳转 | 不初始化各模块数据结构；各模块首次进入使用硬编码示例数据（projectId 为空） |
| 剧本生成 | 三栏 UI、AI 输入区、`generateScript` | 结果仅纯文本追加，**不解析为结构化幕次**；不按项目题材/幕数生成整部剧本 |
| 分镜生成 | 网格/列表 UI、单镜头重新生成 | **"AI 生成全部"按钮无逻辑**；不从完整剧本批量生成镜头；无镜头解析器 |

**核心结论**：UI 层已具备，缺的是"配置生效 + 结构化生成 + 数据打通"三件事。

## 3. 范围界定

### 本期做

- API 配置与 AI 服务打通（配置真实生效、连通性测试、错误提示）
- 新建项目初始化各模块数据骨架
- 剧本生成：结构化输出 → 解析为幕次（场景/地点/时间/角色/台词）→ 落库 → 展示
- 分镜生成：从剧本批量生成镜头描述 → 解析为镜头 → 落库 → 状态流转
- 端到端冒烟测试与联调

### 本期不做

- 文生图（分镜图真实图片，沿用占位图）
- 配音 / 视频合成（已另有链路）
- 流式输出（可作后续增强）
- 云同步 / 账号（Phase 2 范围）

---

## 4. 总体方案

### 4.1 数据流

```
API 配置页 ──读写──▶ AIModelSettings（localStorage，唯一事实来源）
                          │
新建项目 ──▶ 初始化骨架：episodes[scenes] + 空 shots/characters/dubbing
                          │
剧本页 ──▶ 题材/幕数/上下文 ──▶ generateScript（按用途路由模型）
                          │
              解析器 parseScriptToEpisodes ──▶ episodes[] 落库
                          │
分镜页 ──▶ 选中幕剧本 ──▶ generateShots（storyboard 模型）
                          │
              解析器 parseShotsToShots ──▶ shots[] 落库
```

### 4.2 剧本结构化输出格式（Prompt 约定）

模型按以下格式输出，解析器按此解析，格式不符时容错降级：

```text
### 第1幕 ｜ 场景：内景·咖啡馆·白天 ｜ 人物：林晓、陈诺

【场景描述】
咖啡馆全景，阳光透过玻璃窗洒在桌面上……

林晓：（慌乱）对不起！我真的不是故意的。
陈诺：没事。
```

解析规则：

- `### 第N幕` → 幕次标题、`sceneNumber`
- `场景：内景/外景·地点·时间` → type / location / time
- `人物：A、B` → characters
- `【场景描述】` 之后为正文；正文含台词行（`角色名：（情绪）台词`）与动作描写
- 每个幕次正文保留完整原文，`words` 为字数统计

### 4.3 分镜输出格式（Prompt 约定）

每行一个镜头，竖线分隔，方便解析与容错：

```text
全景|平视|3.5|咖啡馆全景，阳光透过玻璃，林晓端着咖啡走向镜头
中景|微俯|2.0|林晓脚下绊倒，咖啡杯失手飞出
特写|平视|1.5|咖啡泼洒瞬间，溅射到西装上
```

解析规则：

- 格式不符的行丢弃；`duration` 解析失败默认 3s
- `status` 统一为 `done`（描述已生成），图片仍为占位

### 4.4 错误处理规范（AI 服务层统一）

| 场景 | 提示 |
|------|------|
| 未配置 Key / 未启用模型 | "请先在 AI 模型配置中填写并启用对应用途的模型" |
| 401 / 403 | "API Key 无效或无权限，请检查" |
| 429 | "请求过于频繁，请稍后重试" |
| 超时（60s） | "请求超时，请检查网络或稍后重试" |
| 网络失败 | "网络连接失败，请检查网络" |
| 输出格式无法解析 | 保留原文 + 提示"生成结果格式异常，已保存为纯文本，可手动整理" |

---

## 5. 任务分解

### Phase A：API 配置打通（P0，最优先）

| 编号 | 任务 | 涉及文件 | 验收 |
|------|------|----------|------|
| A1 | 以 `ai-models.ts` 为唯一配置源，删除/废弃 ApiConfig 中硬编码模型列表 | `src/components/ApiConfig.tsx` | 配置页读写 `loadModelSettings/saveModelSettings` |
| A2 | 内置常用服务商预设：DeepSeek、通义千问、智谱 GLM、OpenAI、自定义 | `src/services/ai-models.ts` | 一键选择预设即填好 baseUrl/model |
| A3 | Key 输入与展示优化（遮罩、仅本地保存提示） | `ApiConfig.tsx` | Key 不回显明文；有安全提示 |
| A4 | 模型连通性测试：对启用模型发最小请求，显示成功/失败/耗时 | `ApiConfig.tsx`、`ai-openai.ts` | 每个模型可单独测试并显示结果 |
| A5 | 统一错误处理与 60s 超时 | `ai-openai.ts`、`ai.ts` | 按 4.4 规范返回中文错误 |

### Phase B：新建项目

| 编号 | 任务 | 涉及文件 | 验收 |
|------|------|----------|------|
| B1 | `addProject` 时按 `scenes` 初始化 episodes 骨架（标题/幕号，空正文） | `store/useProjectStore.ts` | 新项目含 N 个空幕次 |
| B2 | 初始化空 shots / characters / dubbing 模块数据 | `store/useProjectStore.ts`、`data/storage.ts` | 各模块首次进入显示空状态而非示例数据 |
| B3 | 删除项目时清理全部模块数据 | `store/useProjectStore.ts` | 复用 `clearAllProjectData` |
| B4 | 新建后跳转剧本页（保留现有跳转）+ 生成空状态引导 | `App.tsx`、`ScriptEditor.tsx` | 新项目进入剧本页显示"开始生成"引导 |

### Phase C：剧本生成

| 编号 | 任务 | 涉及文件 | 验收 |
|------|------|----------|------|
| C1 | 编写剧本解析器 `parseScriptToEpisodes(text)`（含容错） | `src/services/script-parser.ts`（新） | 单测通过：正常/缺字段/空文本 |
| C2 | 完善 `generateScript` Prompt：按题材/幕数/风格输出结构化格式 | `src/services/ai-openai.ts` | 输出符合 4.2 格式 |
| C3 | "生成完整剧本"：按项目题材+幕数一键生成 → 解析 → 写入 episodes | `ScriptEditor.tsx` | 生成后幕次列表更新、正文可编辑 |
| C4 | 底部 AI 输入区改造：生成结果解析后写入当前幕（追加）或新建幕 | `ScriptEditor.tsx` | 输入区生成后正文/幕次正确落库 |
| C5 | 单幕生成/续写：以当前幕 context 调用 | `ScriptEditor.tsx` | 生成内容与当前幕衔接 |
| C6 | 润色保留结构：polish 后重新解析同步 | `ScriptEditor.tsx`、`script-parser.ts` | 润色后幕次结构不丢失 |
| C7 | 字数统计与自动保存（已有保存逻辑，补充幕级字数刷新） | `ScriptEditor.tsx` | 字数实时更新 |

### Phase D：分镜生成

| 编号 | 任务 | 涉及文件 | 验收 |
|------|------|----------|------|
| D1 | 编写分镜解析器 `parseShotsToShots(text, scene)`（含容错） | `src/services/shot-parser.ts`（新） | 单测通过：正常/格式不符行丢弃 |
| D2 | 完善 `generateShots` Prompt：按 4.3 格式批量输出镜头 | `src/services/ai-openai.ts` | 输出可解析 |
| D3 | "AI 生成全部"按钮绑定逻辑：选中幕剧本 → 批量生成该幕镜头 | `Storyboard.tsx` | 点击后生成并写入 shots |
| D4 | 整幕批量状态：幕级 loading、逐镜头写入、完成态 | `Storyboard.tsx` | 状态流转 pending→generating→done |
| D5 | 单镜头重新生成（现有逻辑接解析器 + 错误提示） | `Storyboard.tsx` | 单镜头 desc 更新 |
| D6 | 镜头与幕次/项目关联（projectId、scene 正确写入） | `Storyboard.tsx`、`shot-parser.ts` | 切换项目数据隔离 |

### Phase E：验证与联调

| 编号 | 任务 | 验收 |
|------|------|------|
| E1 | mock 链路冒烟：新建项目 → 生成剧本 → 解析 → 批量分镜 | 数据全部落库且结构正确 |
| E2 | 真实 API 联调：使用 DeepSeek 或通义 Key 跑通剧本+分镜 | 与 mock 行为一致 |
| E3 | 错误场景：未配置 Key / 错误 Key / 网络失败 | 显示 4.4 规范提示 |
| E4 | typecheck / build / CI 全绿 | 无类型错误 |
| E5 | 更新 README 与任务计划 | 文档与实现一致 |

---

## 6. 里程碑与时间预估（单人全职）

| 里程碑 | 内容 | 预估 |
|--------|------|------|
| M1 | Phase A：API 配置打通 | 2~3 天 |
| M2 | Phase B：新建项目初始化 | 0.5~1 天 |
| M3 | Phase C：剧本生成 | 2~3 天 |
| M4 | Phase D：分镜生成 | 2~3 天 |
| M5 | Phase E：验证联调 | 1~2 天 |

**合计：约 8~12 个工作日（1.5~2 周）**

建议执行顺序：A → B → C → D → E，A 是前置依赖（配置不生效则后续无法真实联调）。

## 6.1 执行进度（2026-08-06）

- [x] M1 Phase A：API 配置打通（配置页重写、DeepSeek/通义/智谱/OpenAI 预设、连通性测试、统一错误处理与超时）
- [x] M2 Phase B：新建项目初始化数据骨架（episodes/shots/dubbing/characters），删除联动清理
- [x] M3 Phase C：剧本生成（解析器 + 整部生成 + 单幕续写 + 润色保结构 + 编辑器内容同步修复）
- [x] M4 Phase D：分镜生成（解析器 + "AI 生成全部"逐幕批量 + 单镜头重生成 + 状态流转）
- [x] M5 Phase E：解析器冒烟测试通过、typecheck/build 通过；真实 API 联调待用户提供 Key 后执行

---

## 7. 验收标准（本期完成定义）

- [ ] 在配置页填入 DeepSeek / 通义 Key 并启用后，剧本生成真实调用该模型
- [ ] 新建项目自动创建对应幕数的空幕次，各模块无示例数据串扰
- [ ] 一键生成剧本后，幕次列表、场景信息、台词正文正确展示并可编辑
- [ ] 剧本润色/续写后幕次结构保持完整
- [ ] 分镜"AI 生成全部"从选中幕剧本批量生成镜头，状态流转正确
- [ ] 单镜头可重新生成，失败时提示友好错误
- [ ] 切换项目数据隔离，重启后数据可恢复
- [ ] 未配置 Key 时操作有明确引导
- [ ] mock 与真实 API 两条链路均通过冒烟测试

---

## 8. 风险与依赖

| 风险 | 应对 |
|------|------|
| 模型输出格式不稳定 | 解析器容错 + 原文保留兜底 + 人工可编辑 |
| 用户没有海外模型 Key | 内置 DeepSeek / 通义 / 智谱等国内服务商预设 |
| API 成本不可控 | 生成前展示预估字数；批量生成设数量上限；Prompt 限长 |
| 与后续配音/合成模块耦合 | 本期输出的数据结构与现有 types.ts 一致，不额外建模 |
| 沙箱环境无法访问外网 | 联调由用户本地提供 Key 执行；仓库内置 mock 冒烟测试 |

---

*本计划为 v1.0，评审通过后按 M1 → M5 顺序执行。*
