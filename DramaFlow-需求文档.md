# DramaFlow AI 短剧生成提效工具 — 产品需求文档

> 版本：v0.4（新增项目总览页面）  
> 日期：2026-08-04  
> 状态：待评审，未进入编码阶段

---

## 1. 项目概述

### 1.1 产品定位
DramaFlow 是一款面向 AI 短剧创作者的**桌面端生产流程工具**，以项目为单位，提供"项目总览 + 剧本创作 → 分镜生成 → 角色管理 → 场景配音 → 视频合成"全链路管理。

### 1.2 核心目标
- 用 AI 辅助完成剧本创作、分镜生成、角色定妆、场景配音与视频合成。
- 以"项目"为维度组织工作流，支持多项目并行管理。
- 项目总览页集中展示所有项目及其进度，快速掌握全局状态。
- 5 步工作流依次推进，每个步骤有明确的完成/生成中/待处理状态。
- 桌面端体验，左侧项目列表 + 顶部导航 + 主内容区。

### 1.3 目标用户
- AI 短剧独立创作者 / MCN 内容团队
- 需要批量产出 1–3 分钟竖屏短剧的编剧、导演、后期
- 对"AI 生成 + 人工精修"混合工作流有需求的用户

---

## 2. 总体架构

### 2.1 应用形态
- **桌面端应用**（优先 Electron + React，可复用现有 HTML/CSS，或 Tauri 减包体积）。
- 默认窗口尺寸参考设计稿：**1440 × 900** 及以上。
- 单窗口单页应用，通过 Tab 切换工作流步骤。

### 2.2 布局结构

应用有**两种视图模式**：

**模式一：项目总览（默认进入）**

```
┌──────────┬──────────────────────────────────────────────┐
│ Sidebar  │  Topbar（项目总览 · DramaAI）                 │
│          ├──────────────────────────────────────────────┤
│ 项目列表 │                                              │
│          │  项目总览 — 所有项目卡片 + 进度               │
│ 工作流   │                                              │
│ 步骤     │                                              │
│          │                                              │
└──────────┴──────────────────────────────────────────────┘
```

**模式二：项目工作流（点击项目进入）**

```
┌──────────┬──────────────────────────────────────────────┐
│ Sidebar  │  Topbar（项目名 + 面包屑 + 保存/导出）        │
│          ├──────────────────────────────────────────────┤
│ 项目列表 │  Tab Nav（剧本创作/分镜生成/角色管理/场景配音/视频合成）│
│          ├──────────────────────────────────────────────┤
│ 工作流   │                                              │
│ 步骤     │  主内容区（各步骤对应组件）                    │
│          │                                              │
└──────────┴──────────────────────────────────────────────┘
```

- 点击 Sidebar 项目列表中的"项目总览"或点击 Topbar 返回，切换到项目总览视图。
- 点击某个项目进入该项目的 5 步工作流视图。
- 工作流中点击 Sidebar 其他项目，直接切换项目的工作流。

### 2.3 页面/视图清单

| 视图 | 名称 | 核心职责 |
|------|------|----------|
| 项目总览 | Project Overview | 所有项目卡片展示、进度总览、新建项目、快捷筛选 |
| 剧本创作 | Script Editor | AI 生成剧本、幕次管理、台词编辑 |
| 分镜生成 | Storyboard | 镜头拆解、网格/列表视图、一键生成分镜图 |
| 角色管理 | Characters | 角色卡、人物档案、声音设置、形象模型 |
| 场景配音 | Dubbing | 按台词生成配音、情绪/语速调节、批量配音 |
| 视频合成 | Video Synthesis | 场次片段管理、输出设置、后期处理、一键合成导出 |

### 2.4 5 步工作流

| 步骤 | Tab 名称 | 状态 | 核心职责 |
|------|----------|------|----------|
| STEP 1 | 剧本创作 | done | AI 生成剧本、幕次管理、台词编辑 |
| STEP 2 | 分镜生成 | done | 镜头拆解、网格/列表视图、一键生成分镜图 |
| STEP 3 | 角色管理 | active | 角色卡、人物档案、声音设置、形象模型 |
| STEP 4 | 场景配音 | pending | 按台词生成配音、情绪/语速调节、批量配音 |
| STEP 5 | 视频合成 | pending | 场次片段管理、输出设置、后期处理、一键合成导出 |

### 2.5 核心工作流

```
[项目总览] 查看所有项目 → 新建项目 / 点击项目进入
     │
     ▼
[项目工作流]
STEP 1 剧本创作 → 幕次列表 + AI 生成剧本 + 编辑台词
     │
     ▼
STEP 2 分镜生成 → 拆解为镜头 → 网格/列表预览 → AI 生成分镜图
     │
     ▼
STEP 3 角色管理 → 角色卡 + 人物档案 + 声音设置 + 形象模型
     │
     ▼
STEP 4 场景配音 → 台词列表 + 情绪/语速/音量 + 批量配音
     │
     ▼
STEP 5 视频合成 → 场次片段 + 输出设置 + 后期处理 + 合成导出
```

---

## 3. 全局交互与视觉规范

### 3.1 设计语言

| 类别 | 值 |
|------|-----|
| **主色调** | Amber 琥珀色 `#E69500`，辅助 `#FFF3D0`（浅底）、`#FFF8EC`（选中态） |
| **中性色** | 文字 `#1A1D24`、次要文字 `#5A6070`、弱化文字 `#9AA0B0`、背景 `#F5F6F8` |
| **边框** | `#E4E7EE`（默认）、`#F5C842`（选中态高亮） |
| **状态色** | 成功 `#16A34A` / `#ECFDF5`、警告/进行中 `#E69500` / `#FFF3D0`、信息 `#2563EB` / `#EFF4FF` |
| **字体** | 标题 `Outfit`、正文 `Inter`、代码/等宽 `JetBrains Mono` |
| **圆角** | 小 3-4px、中 5px、卡片 8px |
| **阴影** | 卡片 `0 1px 3px rgba(0,0,0,0.04)`、按钮 `0 1px 4px rgba(230,149,0,0.3)` |

### 3.2 全局布局组件

#### Sidebar（左侧边栏，宽度 216px）
- **Logo 区**：DramaAI 标识 + 版本号 `v2.4.1`。
- **项目列表**：顶部"项目总览"入口（固定），下方展示多个项目，每项显示项目名、幕数、时长、更新时间。选中项高亮（琥珀色左边框 + 浅底背景）。底部"新建项目"按钮。
- **工作流步骤**：5 个步骤纵向排列，每步显示 STEP 编号、步骤名、状态圆点（绿色=完成、琥珀色=当前、灰色=待处理）。选中项高亮。
- **底部状态栏**：AI 引擎在线状态 + 今日生成统计。

#### Topbar（顶部栏，高度 44px）
- 左侧：当前项目名 `·` 当前 Tab 名称。
- 右侧："保存草稿"按钮（次要） + "导出项目"按钮（琥珀色主按钮）。

#### Tab Nav（Tab 导航栏，高度约 36px）
- 5 个 Tab 横向排列：剧本创作 / 分镜生成 / 角色管理 / 场景配音 / 视频合成。
- 每个 Tab 显示图标 + 标签名 + 状态标签（如"完成"）。
- 当前 Tab 底部显示琥珀色下划线，文字加粗。

### 3.3 通用组件
- **Button**：Primary（琥珀色填充 `#E69500`）、Secondary（白底边框 `#E4E7EE`）、Ghost（透明）。
- **Tag**：灰色背景 `#EEF0F4`，文字 `#5A6070`，圆角 3px，用于场景类型、角色标签等。
- **StatusBadge**：小标签，颜色根据状态变化（完成=绿、生成中=琥珀、待处理=灰）。
- **SectionLabel**：等宽字体大写，`#9AA0B0`，用于面板分区标题。
- **ProgressBar**：灰色底 + 琥珀色填充，用于进度展示。
- **工时输入**：`#F5F6F8` 背景，圆角 4px，聚焦无特殊样式。

---

## 4. 功能模块详细需求

### 4.0 项目总览（Project Overview）

应用的默认入口页面，展示用户所有项目的全局视图。

#### 4.0.1 顶部统计卡片
4 个核心指标横向排列：
- **全部项目**：项目总数，附带环比增长率。
- **进行中**：状态非"已完成"的项目数。
- **本月成片**：本月完成视频合成的项目数。
- **总幕数**：所有项目累计幕次/镜头数。

#### 4.0.2 筛选与搜索
- **筛选 Tab**：全部项目 / 进行中 / 已完成 / 最近编辑。
- **搜索框**：按项目名称模糊搜索。
- **排序**：按更新时间 / 创建时间 / 进度排序。

#### 4.0.3 项目卡片网格
- 网格布局，3 列（大屏）/ 2 列（中屏）/ 1 列（小屏）。
- 每个卡片包含：
  - **封面缩略图**：16:9 占位，显示项目封面或第一幕缩略图，hover 显示播放遮罩。
  - **项目名称**：Outfit 字体，14px 加粗。
  - **题材标签**：甜宠 / 逆袭 / 悬疑 / 玄幻 / 都市（琥珀色边框胶囊）。
  - **工作流进度条**：5 段式进度条，每段对应一个 STEP（剧本/分镜/角色/配音/合成），已完成=绿色、进行中=琥珀色、未开始=灰色。
  - **当前阶段文案**：如"剧本创作中 · 完成 3/5 幕"。
  - **状态标签**：草稿 / 进行中 / 已完成。
  - **最近更新时间**：等宽字体显示。
- 点击卡片进入该项目的 5 步工作流。

#### 4.0.4 新建项目
- 卡片网格末尾放置"新建项目"卡片（虚线边框 + 居中的"+"图标 + "新建项目"文字）。
- 点击弹出新建项目对话框：
  - 项目名称
  - 题材类型下拉
  - 目标幕数（默认 24）
  - 竖屏/横屏选择
  - 确认后跳转至剧本创作 STEP 1。

#### 4.0.5 快速入口
- 页面顶部或侧边放置快捷入口：
  - "从灵感开始" → 新建项目并进入剧本创作
  - "从剧本导入" → 新建项目并进入剧本导入
  - "从模板开始" → 选择预置模板创建项目

#### 4.0.6 空状态
- 无项目时显示引导插图 + "还没有项目，开始创建你的第一部短剧吧" + "新建项目"按钮。

---

### 4.1 STEP 1 — 剧本创作（Script Editor）

#### 4.1.1 三栏布局
- **左侧幕次列表**（宽度 210px）：列出所有幕次，每幕显示标题、字数、类型标签（内景/外景）、时间标签（白天/傍晚/夜晚/深夜）、地点、出场角色标签。选中项琥珀色高亮 + 左边框。底部显示总字数进度条与目标字数。
- **中间剧本编辑区**：等宽字体（JetBrains Mono），行高 2.1，浅灰背景 `#FAFBFC`。顶部工具栏显示当前幕次标题、类型/地点/时间标签，以及"格式化"和"AI 润色"按钮。
- **右侧属性面板**（宽度 192px）：场景属性、出场角色列表（圆形头像 + 角色名）、AI 建议（如"加强情感冲突"）。

#### 4.1.2 底部 AI 输入区
- 快捷建议按钮：续写下一幕、优化对白节奏、增加冲突张力、补充人物细节。
- 文本输入框 + "AI 生成"按钮（生成中显示"生成中…"并禁用）。

#### 4.1.3 新增幕次
- 顶部"+"按钮可新增幕次，自动编号。

---

### 4.2 STEP 2 — 分镜生成（Storyboard）

#### 4.2.1 视图切换
- 网格/列表两种视图模式，通过分段按钮切换。
- 显示总镜头数，右上角"AI 生成全部"按钮。

#### 4.2.2 镜头卡片（网格模式）
- 每个卡片：16:9 占位缩略图 + 镜头编号 `#001` + 状态标签 + 镜头类型/角度标签 + 描述文字（最多 2 行）+ 时长。
- 三种状态：完成（显示缩略图）、生成中（loading 动画）、待生成（占位图标 + "生成分镜"按钮）。

#### 4.2.3 镜头卡片（列表模式）
- 横向排列：缩略图（80×45）+ 镜头编号 + 类型/角度 + 状态标签 + 描述 + 时长。

#### 4.2.4 右侧详情面板（宽度 256px）
- 选中镜头时显示：镜头编号、镜头类型、预览图（带状态标签）。
- 镜头参数：镜头类型、拍摄角度、时长、所属幕次。
- 镜头描述可编辑文本域 + "重新生成"按钮。

#### 4.2.5 镜头状态
- 完成（绿色）/ 生成中（琥珀色）/ 待生成（灰色）。

---

### 4.3 STEP 3 — 角色管理（Characters）

#### 4.3.1 角色列表（宽度 196px）
- 每个角色：圆形头像（Unsplash 图片）+ 角色名 + 角色定位（男主角/女主角/配角）。
- 选中项琥珀色高亮 + 左边框。

#### 4.3.2 角色详情头部
- 大圆形头像 + 角色名（Outfit 字体，16px 加粗）+ 角色定位标签（蓝色边框胶囊）。
- 个性标签（#霸总 #冷淡 #深情）。
- 统计数据：出场幕数、台词条数（琥珀色大数字）。

#### 4.3.3 人物档案 Tab
- 左右双栏布局。
- **左栏**：基本信息（身份/年龄/性格/口头禅等键值对）、人物描述文本域。
- **右栏**：AI 人设建议卡片（黄色背景 `#FFFDF0`，含具体建议 + "应用建议"按钮）、台词类型分布（4 个色块：情感类/冲突类/日常对话/独白，含百分比）。

#### 4.3.4 声音设置 Tab
- 当前声音：播放/暂停按钮 + 声音名称 + 进度条 + 时长。
- 可用声音库：列表展示，每项含图标 + 名称 + 描述 + 状态标签（使用中/可选）。选中项琥珀色边框高亮。

#### 4.3.5 形象模型 Tab
- 形象参考：4 列网格，首张为选中形象（琥珀色边框），其余为"+"添加占位。
- 风格设置：2×2 网格，展示渲染风格/服装风格/年龄段/体型。
- "AI 生成形象"按钮。

---

### 4.4 STEP 4 — 场景配音（Dubbing）

#### 4.4.1 顶部进度条
- 配音进度百分比 + 已完成/总数 + "批量配音"按钮。

#### 4.4.2 台词列表
- 每行包含：台词编号（等宽字体）、所属幕次、角色色点（不同角色不同颜色）、角色名（带颜色） + Take 标签（如有多次录制）、台词文本、状态标签（完成/生成中/待配音）、时长。
- 已完成项显示播放按钮，待配音项显示"生成"按钮。
- 选中行琥珀色背景 + 左边框。

#### 4.4.3 右侧详情面板（宽度 252px）
- 选中台词时显示：台词编号 + 完整台词文本。
- **情绪设置**：4 个情绪按钮（慌乱/焦虑/惊喜/平静），选中态蓝色边框高亮。
- **语速**：滑块 0.5× ~ 2.0×，显示当前值。
- **音量**：滑块 0-100。
- **波形预览**：已完成项显示模拟波形柱状图，未生成显示"未生成"。
- 底部"AI 配音"或"重新生成"按钮。

#### 4.4.4 角色颜色映射
- 林晓 `#2563EB`（蓝色）、陈诺 `#E69500`（琥珀色）、秘书 `#7C3AED`（紫色）、董事长 `#16A34A`（绿色）。

---

### 4.5 STEP 5 — 视频合成（Video Synthesis）

#### 4.5.1 左侧预览 + 片段列表
- **视频预览区**：竖屏 9:16 播放器，居中展示，底部进度条 + 时间戳，支持播放/暂停。
- **场次片段列表**：每项显示缩略图 + 场次名称 + 片段编号 + 镜头数 + 时长 + 状态标签（就绪/处理中/待处理）。

#### 4.5.2 右侧面板（宽度 272px）

**输出设置**
- 输出分辨率：1080×1920（竖屏 9:16）
- 帧率：30fps（标准）
- 码率：8 Mbps（高质量）
- 格式：MP4/H.264（兼容性强）

**后期处理开关**
- AI 色调统一（默认开启）
- 背景音乐（默认开启）
- 字幕自动生成（默认开启）
- 片头片尾（默认关闭）
- 水印去除（默认关闭）
- 每个开关为琥珀色 toggle 滑块。

**合成进度**
- 总进度百分比 + 进度条。
- 子步骤状态列表：素材加载 / 镜头剪辑 / 配音合并 / 色调处理 / 字幕渲染 / 最终封装，每项显示圆点（绿=完成、琥珀=进行中、灰=未开始）+ 百分比。
- "开始合成"按钮（琥珀色），合成中按钮变灰并显示"合成中…"，完成后变为"下载成品"。
- 完成时显示绿色提示卡片："合成完成 · 总时长 X:XX.X"。

---

## 5. 数据模型（核心实体）

### 5.1 项目（Project）
```
- id, name, scenes, duration, updatedAt
- episodes: Episode[]
- characters: Character[]
- shots: Shot[]
- dubbingLines: DubbingLine[]
- clips: SynthesisClip[]
- synthesisConfig: SynthesisConfig
```

### 5.2 幕次（Episode/Scene）
```
- id, number, title, type(内景/外景), location, time, chars[], words, script
```

### 5.3 角色（Character）
```
- id, name, role, age, tags[], desc, voice, model
- scenes, lines, attrs: { 身份, 年龄, 性格, 口头禅 }
- avatarUrl
```

### 5.4 镜头（Shot）
```
- id, number, sceneId, type, angle, duration, desc, status
- thumbnailUrl
```

### 5.5 配音台词（DubbingLine）
```
- id, charName, sceneId, text, status, duration, take
- emotion, speed, volume
```

### 5.6 合成片段（SynthesisClip）
```
- id, sceneId, shots, duration, status, thumbUrl
```

### 5.7 合成配置（SynthesisConfig）
```
- resolution, fps, bitrate, format
- postProcessing: { colorGrading, bgm, subtitles, introOutro, watermarkRemoval }
- progress: { total, steps: [{ name, percent }] }
```

---

## 6. 非功能需求

### 6.1 性能
- 首屏加载 ≤ 3s（桌面端本地资源）。
- 幕次列表、镜头列表滚动流畅。
- 视频合成模拟异步进度，不阻塞 UI。

### 6.2 离线/本地优先
- 项目数据、剧本、角色配置、镜头数据本地持久化（SQLite / 本地 JSON + 文件）。
- AI 生成依赖网络，但本地编辑、预览可离线进行。

### 6.3 扩展性
- AI 模型接口可插拔：剧本生成、分镜图生成、配音、视频合成均通过统一接口接入。
- 预留 API Key 配置入口。

### 6.4 可访问性
- 支持键盘导航、焦点可见。

### 6.5 设计一致性
- 严格使用设计稿中的颜色体系（Amber 主色调），避免硬编码颜色。
- 所有交互元素需补全 `data-dom-id`，保证设计与实现一致。

---

## 7. 技术选型建议（待评审）

| 方案 | 优点 | 缺点 |
|------|------|------|
| **Electron + React** | 生态成熟、可复用现有代码、跨平台 | 包体积大 |
| **Tauri + React** | 包体积小、性能好、Rust 后端 | 生态相对年轻 |

**推荐**：优先 **Electron + React**，Figma 生成代码已基于 React 19 + Vite + Tailwind CSS v4，迁移成本最低。

---

## 8. 里程碑规划

| 阶段 | 目标 | 产出 |
|------|------|------|
| M1 | 项目骨架 + Sidebar + Topbar + 项目总览页 + 项目切换 | 可运行的桌面壳 + 项目总览 |
| M2 | 剧本创作模块（幕次列表 + 编辑器 + 属性面板 + AI 输入） | 剧本创作可用 |
| M3 | 分镜生成模块（网格/列表 + 详情面板 + 状态管理） + 角色管理模块 | 分镜 + 角色可用 |
| M4 | 场景配音模块（台词列表 + 参数面板 + 批量配音） | 配音可用 |
| M5 | 视频合成模块（片段管理 + 输出设置 + 后期处理 + 合成进度） | 全流程可用 |
| M6 | AI 模型接入 + 本地持久化 + 联调 + 优化 | 完整产品 |

---

## 9. 与旧版需求文档的关键差异

| 差异点 | 旧版（v0.2） | 新版（v0.4 基于 Figma + 新增项目总览） |
|--------|-------------|------------------------|
| 页面数 | 7 个 | 1 个项目总览 + 5 个 Tab |
| 仪表盘 | 独立页面 | 改为"项目总览"，所有项目卡片 + 进度 + 筛选 |
| 角色管理位置 | 分镜之前 | 分镜之后 |
| 视频生成 + 后期 | 两个独立页面 | 合并为"视频合成"一个 Tab |
| 后期处理 | 配音/字幕/BGM/转场/混音 | 场景配音独立为 STEP 4，合成阶段含色调/BGM/字幕/片头片尾/水印开关 |
| 主色调 | Indigo-Violet `#4F46E5` | Amber `#E69500` |
| 字体 | Inter + PingFang SC | Outfit + Inter + JetBrains Mono |

---

## 10. 风险与待确认事项

1. AI 模型接入：剧本生成、分镜图生成、配音（TTS）、视频合成各用哪个 API？
2. 配音引擎：是否支持多情绪、多音色？是否支持音色克隆？
3. 视频合成：是纯前端 FFmpeg 合成，还是后端渲染？
4. 导出格式：是否需要导出到剪映 / Premiere / Final Cut 的工程文件？
5. 账号与云同步：是否需要登录后云端同步项目？
6. 桌面平台：优先 Windows / macOS / 同时支持？
7. 竖屏/横屏：默认竖屏 9:16，是否兼容横屏 16:9？

---

## 11. 验收标准

- [ ] 项目总览页展示所有项目卡片，支持筛选、搜索、新建项目，项目进度一目了然。
- [ ] 5 个 Tab 均可在桌面端正常切换、导航流畅。
- [ ] 支持多项目切换，项目数据持久化。
- [ ] 剧本创作支持幕次列表切换、编辑台词、AI 输入。
- [ ] 分镜生成支持网格/列表视图、镜头状态流转、详情编辑。
- [ ] 角色管理支持档案/声音/形象三个 Tab、AI 人设建议。
- [ ] 场景配音支持台词列表、情绪/语速/音量调节、批量配音。
- [ ] 视频合成支持片段管理、输出设置、后期处理开关、合成进度模拟。
- [ ] 项目数据本地持久化，关闭重启后可恢复。
- [ ] 设计风格与 Figma 设计稿一致（Amber 主色调）。

---

*本需求文档基于 Figma 生成的 `AI Short Drama Generator` 代码整理，待你确认后即可进入技术方案与原型开发阶段。*